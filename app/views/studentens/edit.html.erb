 <% if !session[:user_id] %>
U bent niet bevoegd om deze pagina te bekijken
<% else %>
Welkom <strong><%=session[:user_id]%></strong>.
<%= link_to "logout",:controller => "gebruikers", :action => "logout" %>
<h1>Editing studenten</h1>

<%= render 'form' %>

<%= link_to 'Show', @studenten %> |
<%= link_to 'Back', studentens_path %>
<% end %>

