<% if !session[:user_id] %>
U heeft niet de juiste bevoegdheid om deze pagina te bekijken
<% else %>
Welkom <strong><%=session[:user_id]%></strong>.
<%= link_to "logout",:controller => "gebruikers", :action => "logout" %>
<p id="notice"><%= notice %></p>
<table>
<tr>
	<td>
  		<b>id</b>
	</td>
  	<td>
  		<%= @labo.id %>
  	</td>
</tr>
<tr>
	<td>
  		<b>Naam:</b>
	</td>
  	<td>
  		<%= @labo.naam %>
  	</td>
</tr>

<tr>
	<td>
	    <b>Docent_id:</b>
	 </td>
	 <td>
	 	 <%= @labo.docent_id %>
	 </td>
</tr>
</table>

<%= link_to 'Aanpassen', edit_labo_path(@labo) %> |
<%= link_to 'Ga terug', labos_path %>
<% end %>
